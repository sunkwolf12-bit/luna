# 2026-04-22 (MX)

1) **Resumen del día**
- Se ejecutó la automatización nocturna de exportación de RAW.
- Hubo un fallo porque no existía el script esperado `scripts/export_raws.py`; se aplicó un fallback para completar la exportación y dejar listo el RAW para el rebuild.

2) **Decisiones / acuerdos** (sin datos sensibles)
- Para la exportación nocturna de RAW: si falta `scripts/export_raws.py`, usar como fallback el script existente `scripts/batch_export_raws.sh`.
- Mantener un alias con el nombre estándar del día (`memory/raws-daily/YYYY-MM-DD.md`) apuntando al RAW generado, para que los procesos posteriores no fallen por diferencias de nombre.

3) **Pendientes** (accionables)
- Revisar/definir la fuente “oficial” para exportar RAW: crear `scripts/export_raws.py` o actualizar la tarea nocturna para que use el script real disponible.
- Verificar que el proceso de export incremental siga generando el RAW diario con el nombre esperado (alias incluido).

4) **Procedimientos / checklist vigente** (reutilizable)
- Nightly RAW export (MX 23:40):
  - Intentar `python3 scripts/export_raws.py day today`.
  - Si el script no existe o falla, ejecutar fallback: `bash scripts/batch_export_raws.sh`.
  - Confirmar que exista `memory/raws-daily/YYYY-MM-DD.md` (crear alias si el archivo real quedó con otro nombre).

## Personal (fidedigno)
- Por la noche corrió la automatización; se detectó un detalle técnico (script faltante) y se corrigió en el momento para no romper el flujo del rebuild.
