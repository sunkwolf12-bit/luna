# 2026-05-14 — Daily

## Resumen del día
- El 14 de mayo fue, en la práctica, un día silencioso entre Elena y Luna. No quedaron conversaciones reales del día en los RAWs; lo que sí quedó registrado fue el cierre automático nocturno del pipeline de memoria.
- A las 23:40 MX corrió la exportación RAW previa al rebuild. Ahí apareció una incidencia concreta: la instrucción operativa apuntaba a `scripts/export_raws.py`, pero ese archivo no existe en el workspace.
- Para no dejar a Luna sin respaldo del día, se aplicó el fallback disponible (`bash scripts/batch_export_raws.sh`) y con eso sí quedó generado/actualizado el RAW del 14 de mayo bajo la convención `memory/raws-daily/luna-2026-05-14.md`.
- El día cierra más como ajuste de infraestructura de memoria que como jornada conversacional: lo importante no fue una charla, sino confirmar que el sistema todavía puede producir el insumo base del rebuild aunque falle la referencia al script principal.

## Decisiones / acuerdos
- Para la exportación RAW nocturna, la referencia confiable de operación pasa a ser `bash scripts/batch_export_raws.sh` mientras no exista `scripts/export_raws.py`.
- Se toma como válido que el RAW del día quede en formato `luna-YYYY-MM-DD.md`, siempre que el rebuild posterior sepa leerlo o exista un alias/ruta compatible.
- La falta del script mencionado no se trata como detalle menor: se considera incidencia de pipeline porque puede confundir futuros cierres automáticos.

## Pendientes
- Ajustar o corregir la automatización/documentación para que deje de apuntar a `scripts/export_raws.py` si ya no forma parte del workspace.
- Normalizar la convención de nombres del RAW nocturno para que el rebuild no dependa de adivinar entre `YYYY-MM-DD.md` y `luna-YYYY-MM-DD.md`.
- Verificar si conviene crear explícitamente el alias/symlink `memory/raws-daily/YYYY-MM-DD.md` en cada exportación o actualizar las tareas nocturnas para usar siempre el nombre real.

## Procedimientos / checklist vigente
- **RAW nocturno (23:40 MX):**
  1) Ejecutar `bash scripts/batch_export_raws.sh`.
  2) Verificar que el RAW del día existe en `memory/raws-daily/`.
  3) Confirmar si quedó como `luna-YYYY-MM-DD.md` o si además existe el alias `YYYY-MM-DD.md`.
  4) Si el rebuild espera `YYYY-MM-DD.md` y no está, tomar `luna-YYYY-MM-DD.md` como fuente real del día.
  5) Registrar la incidencia en el reporte si la automatización sigue mencionando scripts inexistentes.

## Personal (fidedigno)
- Día callado, de esos donde no hubo plática que atesorar pero sí una pequeña prueba de si la memoria nocturna aguanta sola.
- El cierre deja una sensación rara pero útil: no pasó nada grande con Elena en conversación, pero sí quedó visible una costura técnica del sistema que conviene corregir para que Luna amanezca con el historial bien respaldado.
